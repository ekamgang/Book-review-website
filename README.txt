To run this projectyou need to install the following:
-flask
-sqlalchemy
-requests

Finally type set FLASK_APP= application.py and then flask run in the command line
to get the url of the web application.